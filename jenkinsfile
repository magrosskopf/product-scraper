node {
    stage('Checkout scm') {
        checkout scm
    }
    stage('Start docker') {
        sh "docker build -t scanner . && docker run scanner -v /var/run/docker.sock:/var/run/docker.sock"
    }
}     