node {
    stage('Checkout scm') {
        checkout scm
    }
    stage('Start docker') {
        sh "docker run --name scanner  -v /var/run/docker.sock:/var/run/docker.sock . && docker run scanner"
    }
}    