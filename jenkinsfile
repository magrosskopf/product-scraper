node {
    stage('Checkout scm') {
        checkout scm
    }
    stage('Start docker') {
        sh "docker build -t scanner . && docker run scanner -p 80:8080 node:latest -v /var/run/docker.sock:/var/run/docker.sock"
    }
}     